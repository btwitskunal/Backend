# Copy this file to .env and update the values

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=SCLWebPortal
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0

# Server Configuration
PORT=4000
NODE_ENV=development

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOADS_DIR=uploads
REPORTS_DIR=reports

# Security Configuration
SESSION_SECRET=your-super-secret-session-key-change-this-in-production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
BCRYPT_ROUNDS=12

# CORS Configuration (for production)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Frontend URL (for SSO redirects)
FRONTEND_URL=http://localhost:3000

# SAML SSO Configuration
SAML_ENTRY_POINT=https://login.microsoftonline.com/your-tenant-id/saml2
SAML_ISSUER=http://localhost:4000
SAML_CALLBACK_URL=http://localhost:4000/auth/sso/callback
SAML_LOGOUT_CALLBACK_URL=http://localhost:4000/auth/sso/logout/callback 